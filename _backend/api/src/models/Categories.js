class Users {
  constructor(
    id,
    name,
    friendlyURL,
    photoId,
  ) {
    this.id = id;
    this.name = name;
    this.friendlyURL = friendlyURL;
    this.photoId = photoId;
  }
}

module.exports = Users;
